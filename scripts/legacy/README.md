# Legacy Python Batch Logic

This folder preserves the original Python implementations from `steer/jy`.

Files:
- `build_sessions_py_legacy.py`
- `build_routines_py_legacy.py`
- `build_handoff_py_legacy.py`

Current entrypoints in `/scripts/build_*.py` are compatibility wrappers that forward to Rust binaries (`build_*_rs`).
